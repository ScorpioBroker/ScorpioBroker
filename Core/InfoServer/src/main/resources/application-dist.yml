server:
  port: 27015

spring:
  cloud:
    stream:
      kafka:
        binder:
          brokers: kafka:9092

bootstrap:
  servers: kafka:9092
  
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
